[project]
name = "projects-api-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask-restx>=1.3.0",
    "flask>=3.1.0",
]

[dependency-groups]
dev = [
    "isort>=6.0.0",
    "ruff>=0.9.7",
]
